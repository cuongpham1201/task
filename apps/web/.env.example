# SSO do BACKEND điều phối (giống app văn bản/phê duyệt) → frontend KHÔNG giữ
# client id/secret. Chỉ cần địa chỉ API.
VITE_API_BASE=http://localhost:3001/api/v1

# Dev-only: email gửi kèm khi backend ở chế độ dev (chưa bật SSO, khớp user đã seed).
VITE_DEV_USER_EMAIL=cuongpx@biahalong.com
